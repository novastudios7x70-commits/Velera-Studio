{
  "name": "velora-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Velora Studio background pipeline worker — transcription, audio analysis, visual generation, ffmpeg rendering. Deployed separately from the Next.js app (Railway/Render).",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint src"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@supabase/supabase-js": "^2.47.10",
    "bullmq": "^5.34.6",
    "fluent-ffmpeg": "^2.1.3",
    "ioredis": "^5.4.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg": "^2.1.27",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
